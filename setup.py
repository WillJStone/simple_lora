"""
Basic setup file so that the package can be installed with pip.
"""

from setuptools import setup, find_packages

setup(
    name='simple-lora',
    version='0.1.0',
    description='A Python package for low-rank adaptation',
)
